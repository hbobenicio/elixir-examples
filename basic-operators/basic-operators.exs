#!/usr/bin/env elixir

a = 10/3 # yields 3.3333333333333335
b = div 10, 3 # yields 3
c = rem 10, 3 # yields 1
d = [1, 2, 3] ++ [4, 5, 6] # List concatenation operator. Yields [1, 2, 3, 4, 5, 6]
e = "foo" <> "bar" # String concatenation operator. Yields "foobar"
